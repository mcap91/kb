outcome: blocked
needs:
  - network access to https://httpbin.org (and any external endpoint): DNS resolution fails (getaddrinfo EAI_AGAIN for httpbin.org) and the configured proxy http://127.0.0.1:18787 returns HTTP 403 Forbidden for the CONNECT tunnel; example.com, google.com, and api.ipify.org are equally unreachable